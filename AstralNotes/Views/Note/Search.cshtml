@model List<AstralNotes.DAL.Models.Note>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<!doctype html>
<html>
<head>
    <title>Результат поиска</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width">
</head>
<body>
    <form method="post" asp-controller="Account" asp-action="Logout">
        <input type="submit" value="Выход" />
    </form>

    <form method="post" asp-controller="Note" asp-action="Search">
        <input type="text" name="searchString"></input>
        <input type="submit" value="Поиск" />
    </form>
    @Html.ActionLink("Галавная", "Index", "Home")
    @Html.ActionLink("Создать новую заметку", "Create", "Note")

    <h2>Результат поиска:</h2>
    @foreach (var note in Model)
    {
        @Html.Partial("~/Views/Note/Note.cshtml", note)
    }

</body>
</html>